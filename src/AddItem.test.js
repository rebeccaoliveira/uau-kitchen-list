import React from 'react';
import { shallow } from 'enzyme';
import AddItem from './AddItem';

describe("AddItem", () => {
  it("should render my component", () => {
    const wrapper = shallow(<AddItem />);
    expect(shallow(<AddItem />)
  });
});
